{% extends 'base.html.twig' %}

{% block title %}Iniciar sesión
{% endblock %}

{% block body %}
	<body class="overflow-hidden">
		<video autoplay muted loop poster="build/images/video4.mp4">
			<source src="build/images/video4.mp4" type="video/mp4">
		</video>
		<div class="content">
			<div class="content1">
				<form method="post" class="content2 p-4 rounded">
					<h1 class="h3 mb-3 font-weight-normal text-center">Iniciar sesión</h1>
					<label for="inputEmail" class="mb-3">Email</label>
					<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
					<label for="inputPassword" class="mb-3 mt-3">Contraseña</label>
					<input type="password" name="password" id="inputPassword" class="form-control" required>

					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
					{% if error %}
						<div class="alert alert-danger mt-3">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
					{% endif %}
					<button class="button-steelblue btn-sm btn-sm mt-3" type="submit">Iniciar sesión</button>
				</form>
			</div>
		</div>
	</body>
{% endblock %}
